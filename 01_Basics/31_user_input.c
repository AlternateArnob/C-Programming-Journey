// Now, we will take input from the user and work on it.

#include <stdio.h>

int main()
{
    int x;
    printf("Enter value of x: ");
    scanf("%d", &x); // scanf function is used to take input from user
    printf("The value of x is: %d\n", x);
    return 0;
}
