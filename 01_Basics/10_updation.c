#include <stdio.h>

int main()
{
    // int x; // declaration
    // x = 7; // initialization

    int x = 7; // we can do this in one line
    printf("%d", x); 

    x = x + 5; // updation
    printf("\n%d", x); 

    x = x - 12;
    printf("\n%d", x); 
    return 0; 
}