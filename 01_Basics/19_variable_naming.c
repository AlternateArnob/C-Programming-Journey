// Area of a circle 

#include <stdio.h>

int main()
{
    float radius = 5; 
    // insted of x, r(alphabets) we gave a full word radius
    float pi = 3.1416; 
    float a = pi * radius * radius; 
    printf("The area is : %f", a); 
}
 
// there are some variable naming rules. 
// now study indetail. 